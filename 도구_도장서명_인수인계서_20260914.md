# 인수인계서 — 직장인 도구 1호 「디지털 도장·서명 만들기」 + 화면 결함 2건 (2026-09-14)

작성: Claude (Cowork, 클라우드 세션) · 정본 폴더: `C:\Project_Forms` · 대상 사이트: https://freeforms.kr

## 1. 이번 작업 요약

| 구분 | 내용 | 상태 |
|---|---|---|
| 신규 | `/tools/` 직장인 도구 허브, 상단 메뉴 "직장인 도구 NEW", 푸터 링크 | 로컬 반영 완료 · **배포 대기** |
| 신규 | `/tools/stamp/` 디지털 도장·서명 생성기 (C안 프리셋 6종 + 다듬기 + 손글씨/글꼴 서명) | 로컬 반영 완료 · **배포 대기** |
| 신규 | 서식 상세 "이 서식에 찍을 도장 만들기" 위젯 (B안) — 서명·날인 칸 있는 213종에만 | 로컬 반영 완료 · **배포 대기** |
| 결함 | 계열 칩(이력서 7종 등) 현재 항목이 네모 글자 덩어리로 보임 → 알약 모양 유지, 색만 반전 | 수정 완료 |
| 결함 | 이력서(경력용) 미리보기 사진 오른쪽 잘림 → 사진이 칸보다 커서 넘침. 렌더러가 칸 안에 자동 맞춤 | 수정 + 4종 재빌드 완료 |

## 2. 배포 방법 [로컬 작업]

클라우드에서 만든 파일은 이미 `C:\Project_Forms`에 직접 넣어 두었다. 한 줄만 실행한다.

```powershell
cd C:\Project_Forms
.\deploy_forms.ps1 -SkipBuild -Message "feat(tools): 디지털 도장·서명 생성기, 계열 칩·사진 잘림 수정"
```

`-SkipBuild`인 이유: 사진 잘림 4종은 클라우드에서 이미 재빌드해 `public/files/`와 `catalog.json`을 함께 넣었다.
스크립트가 `build_site.py`(사이트 HTML 재생성) → `validate_catalog.py` → 커밋·푸시 → 배포 확인 → IndexNow 통보까지 한다.

### 배포 후 확인 (라이브)

1. https://freeforms.kr/tools/stamp/ — 이름 입력 → 6종 도장이 즉시 바뀌는지, 웹폰트(구기·나눔명조·검은고딕)가 적용됐는지
   (클라우드에서는 Google Fonts가 막혀 대체 글꼴로만 확인했다. **실제 글꼴 모양은 라이브에서 처음 보는 것**이므로 마음에 안 들면 `assets/stamp.js`의 `FONT` 표에서 글꼴만 바꾸면 된다)
2. `PNG 저장` → `도장_이름.png` 투명 배경으로 내려받히는지 · `JPG 저장` 흰 배경
3. 손글씨 서명: 마우스로 그리기 → 되돌리기/지우기 → PNG 저장(`서명_이름.png`, 획 부분만 잘라 저장)
4. https://freeforms.kr/form/loan-agreement/ — 다운로드 박스 아래 도장 위젯. 이름 입력 → 미니 미리보기 → PNG 저장 → "글꼴·색상 바꾸기 →"가 이름을 들고 도구 페이지로 가는지
5. https://freeforms.kr/form/household-budget/ — 서명 칸 없는 서식에는 위젯이 **없어야** 정상
6. https://freeforms.kr/form/resume-experienced/ — 사진이 칸 안에 들어왔는지, "경력용" 칩이 알약 모양 남색인지
7. 모바일(폰)에서 `/tools/stamp/` — 프리셋 2열, 서명 패드가 손가락으로 그려지는지 (`touch-action:none` 처리함)
8. 구글 서치콘솔 → 사이트맵 재제출 (URL 285 → 287: `/tools/`, `/tools/stamp/` 추가)

## 3. 바뀐 파일

| 파일 | 변경 |
|---|---|
| `assets/stamp.js` | **신규.** 도장 그리기(`FFStamp.draw`), PNG/JPG 저장, 웹폰트 대기, 서명 패드(`FFSign.attach`). 빌드가 `public/assets/`로 복사 |
| `templates/tool_stamp.html` | **신규.** 도구 페이지 (프리셋 갤러리·다듬기·서명·사용법·FAQ·관련 서식). 페이지 전용 CSS·Google Fonts 링크는 `{% block head %}`에 |
| `templates/tools.html` | **신규.** 허브 |
| `templates/download.html` · `templates/_ad.html` | (3단계 병합) 준비 페이지 개편·광고 미리보기 상자 |
| `templates/base.html` | 계열 칩 `.cur` 모양 수정 · `{% block head %}` 추가 · 메뉴 "직장인 도구" · 위젯 CSS · 푸터 링크 · (3단계 병합) data-no-track·`.dlalso`·`.adprev-in` |
| `templates/form.html` | `f.seal`일 때 도장 위젯 + 스크립트 |
| `scripts/build_site.py` | `TOOLS`·`TOOLS_SOON`·`STAMP_PRESETS`·`STAMP_FAQ`·`STAMP_RELATED_IDS`·`TOOLS_UPDATED` 상수, `/tools/`·`/tools/stamp/` 렌더링(WebApplication + FAQPage JSON-LD, 빵부스러기), 사이트맵·llms.txt 반영, assets 복사 |
| `scripts/form_spec.py` | `FormSpec.seal` 속성 + `seal_of()` (sign_line 또는 `(인)`·`(서명`·`날인`·`직인`·`인감` 포함 시 참) |
| `scripts/build_form.py` · `scripts/sync_meta.py` | 카탈로그에 `seal` 필드 기록·동기화 |
| `scripts/render_docx.py` | `_fit_photo()` — 사진을 병합 칸(행 높이 합−2mm, 열 폭−3mm, 자동 맞춤 배율 반영) 안으로 비율 유지 축소 |
| `scripts/SPEC_GUIDE.md` | photo_mm 절에 자동 맞춤 설명, "도장 위젯(seal)도 자동" 절 |
| `catalog/catalog.json` | 239종 전부 `seal` 필드 추가 · 재빌드 4종 파일 크기 갱신 |
| `public/files/{resume-experienced, resume-narrative, resume-newcomer, job-application-form}/` | PDF·DOCX·HWPX·preview-1.webp 재생성 (내용 동일, 미리보기만 달라짐) |
| `masters/{위 4종}/` | DOCX 마스터 재생성 |
| `README.md` | "직장인 도구" 절 |

## 4. 설계 결정 (왜 이렇게 했나)

- **C안(프리셋) + B안(상세 위젯)** 조합. 20~30대 직장인은 도장 옵션을 고르는 게 아니라 서식에 빨리 찍는 게 목적이라 완성본 6개를 먼저 보여주고, 세부 조정은 아래 "다듬기"로 뺐다. 상세 위젯은 다운로드 동선에서 이탈 없이 쓰게 하는 장치.
- **개인정보 0.** 이름·서명은 캔버스에서만 처리되고 전송 코드가 없다 → 개인정보처리방침 수정 불필요. 페이지·FAQ에 이 사실을 명시했다.
- **면책 고지.** 인감·공인전자서명이 아니라는 문구를 도구 페이지 본문·FAQ·푸터 면책에 넣었다. 타인 이름 도장 악용 우려는 고지로 대응(본인 확인은 정적 사이트에서 불가능).
- **애드센스 '얇은 페이지' 회피.** 도구 페이지에 사용법 4단계·FAQ 4문항·관련 서식 10종을 본문으로 두었고, 허브에도 기준 설명을 넣었다. FAQ는 FAQPage 구조화 데이터로도 나간다.
- **웹폰트는 도구 페이지에서만** 내려받는다(상세 위젯은 시스템 글꼴). 상세 페이지 속도를 지키기 위해서다.
- **`seal` 자동 판정.** 명세에 새 필드를 쓰게 하면 에이전트가 빠뜨린다. blocks 텍스트에서 자동으로 뽑아 일일 에이전트가 만드는 서식에도 그대로 적용된다.
- **사진 잘림은 명세가 아니라 렌더러에서** 고쳤다. 4종만 고치면 다음 서식에서 또 생긴다. 1페이지 자동 맞춤으로 행이 낮아지는 경우(이력서 기본형 85% 축소 시 37.5→31.9mm)까지 같은 코드가 막는다.

## 5. 잔여 업무

1. **[로컬] 배포** — 위 2절 한 줄 실행 → 8개 확인 항목 점검.
2. **웹폰트 육안 확인** — 구기(전서체풍)가 도장에 어울리는지. 대안: `Song Myung`(고풍 명조), `Hahmlet`. `assets/stamp.js`의 `FONT.seal`과 `templates/tool_stamp.html`의 Google Fonts 링크 두 곳을 같이 바꾼다.
3. **서치콘솔 사이트맵 재제출**, 빙 IndexNow는 배포 스크립트가 자동 통보(--recent 5 에 도구 URL은 안 들어가므로 `python scripts\ping_indexnow.py --all` 한 번 권장).
4. **다음 도구** (허브에 '준비 중'으로 이미 보임): ① 퇴직금·연차·실수령액 계산기(연도별 요율은 `catalog/rates_2026.json` 같은 파일로 분리, 일일 에이전트가 연 1회 점검) ② 금액 한글 표기·글자수 세기. 추가 절차는 README "직장인 도구" 절.
5. **GA4** — `tool_download` 이벤트(tool=stamp, kind=png|jpg)를 이미 보낸다. 08:30 이메일 보고에 도구 이용 수를 넣으려면 `daily_forms_agent.md` 7절 리포트 항목에 추가.
6. 이전부터 남아 있는 것: 애드센스 신청(색인 50페이지 이상 후, 9/17~19 예정) · 검색 오타 허용(2글자 단위 비교) · 서식 요청 폼 켜기(구글 시트 설정 후 `requests.json`).

## 5-1. 미적용 스크립트 2건 정리 (사용자 결정: 3단계를 현재 파일에 병합)

`C:\Project_Forms`에 남아 있던 `fix_v3.ps1`(fix_20260914.tar.gz)과 `step3_v1.ps1`(step3_20260914.tar.gz)은
오늘 SEO·도구 작업 **이전** 버전의 base.html·form.html·build_site.py를 통째로 덮어쓰는 것이어서, 실행하면 오늘 작업이
되돌아간다. 확인 결과와 처리:

- `fix_v3`(검색 배너 여백·연관 서식 분류 라벨): 이미 로컬 정본에 반영돼 있었다(SEO 이관 때 함께 들어옴). 할 일 없음.
- `step3`(다운로드 준비 페이지 개편): 미적용이었다. 변경분만 뽑아 현재 파일에 **병합 완료** —
  `build_site.py`(FORMS_PREVIEW_ADS 미리보기, `public/download/` 자동 정리, related·다른 형식·noindex),
  `templates/download.html`·`_ad.html`(교체), `base.html`(data-no-track, `.dlalso`, `.adprev-in` CSS), README 광고 절.
  일반 빌드 → 미리보기 빌드(1006페이지) → 일반 빌드(289페이지, download/ 정리) 순으로 검증했다.
- 두 스크립트는 **실행해도 아무것도 하지 않는 안내문으로 교체**했다(실수로 눌러도 안전). 스크립트 2개와 tar.gz 2개는
  지워도 된다. `apply_v3.ps1`·`transfer_20260914.tar.gz`도 이미 적용된 SEO 이관본이므로 같이 지워도 된다.
- 다운로드 준비 페이지는 광고가 꺼져 있는 지금은 생성되지 않는다. 애드센스 단계에서 `ads.json`의
  `download_interstitial: true`로 켜면 그때 나온다.

## 6. 식별 이슈

- 印 글자는 한자라 한글 전용 웹폰트(구기·나눔명조 Google 판)에 없어 시스템 글꼴로 떨어진다. 이름과 印의 글꼴이 약간 달라 보일 수 있다. 문제가 되면 "이름만" 기본값으로 바꾸거나(`tool_stamp.html`의 `st-seal` 기본 `on` 위치), 印을 원형 SVG 글리프로 그려 넣는 방법이 있다.
- `validate_catalog.py`는 클라우드 사본에서 파일이 없어 오류를 냈지만 로컬 정본에서는 전 파일이 있으므로 통과해야 한다. 배포 스크립트 3단계에서 실패하면 재빌드 4종의 파일이 제대로 들어갔는지 `public/files/resume-experienced/` 4개 파일을 먼저 본다.

## 7. 이번 세션의 경험치 (가이드 반영)

- 목업은 라이브 사이트 CSS(`base.html`)를 그대로 가져와 그리면 승인 후 구현과 차이가 없다. Playwright로 PC 1280·모바일 400 두 폭을 찍어 나란히 붙여 보여주는 방식이 잘 통했다.
- Playwright로 캔버스 포인터 입력을 시험할 때는 요소를 먼저 `scroll_into_view_if_needed()` 해야 한다. 뷰포트 밖 좌표로 보낸 마우스 이벤트는 조용히 버려져 "그려지지 않음"으로 오판하기 쉽다.
- `defer` 스크립트에 의존하는 인라인 스크립트는 `DOMContentLoaded` 안에서 시작한다(`defer`는 그 전에 실행됨).
- 표 안 그림은 셀보다 크면 LibreOffice가 밖으로 밀어낸다. 그림 크기는 명세 값이 아니라 렌더러가 칸 크기로 클램프해야 안전하다.
