# 무료서식 다운로드 — 인수인계서

- 작성일: 2026-09-13 (KST)
- 작성: Claude (Cowork 세션)
- 대상: jimmy / 다음 세션의 Claude
- 프로젝트 정본 위치: `C:\Project_Forms`
- 기획서: 프로젝트 문서 `claude/무료서식다운로드_개발기획서_v0.1_20260913.md`

---

## 1. 한 줄 요약

기획서 Phase 0~1을 완료하고 Phase 3(초기 서식 확충)의 절반가량을 앞당겨 끝냈다.
**서식 46종이 PDF·DOCX·HWPX 3형식으로 완성되었고, 사이트 63페이지가 생성되어 검증을
통과**했다. 남은 것은 **GitHub·Cloudflare 연결과 도메인 연결(관리자 계정 작업)**, 그리고
**에이전트 스케줄 작업 등록**이다.

---

## 2. 확정된 의사결정

| No | 사항 | 결정 | 근거 |
|---|---|---|---|
| D1 | 한글 형식 | **HWPX로 제공** | 구형 .hwp는 한컴오피스 없이 생성 불가. HWPX는 개방형 표준(KS X 6101)이며 한글 2010 이상에서 열린다 |
| D2 | 승인 무응답 정책 | **12:00까지 무응답이면 당일 생성 없음**, 후보는 다음 날 재상정 | 안전 우선. 2주 시범운영 후 재검토 |
| D3 | 도메인 | **freeforms.kr** (사용자 직접 구매) | 사용자 확정 (2026-09-13). `build_site.py`의 `SITE_URL`에 반영 완료 |
| D4 | 카테고리·대표 10종 | **기획안 채택.** 기업 하위는 "회계·**세무**"로 명명(사용자 폴더 기준) | 사용자가 만든 폴더 트리를 따름 |
| D5 | 다운로드 통계 | Phase 5(오픈 후 1개월)에 도입 | 기획서 12.2 |
| D6 | 광고 | 월 방문 1만 도달 후 검토 | 기획서 12.2 |
| 추가 | 서식 제작 주체 | **Claude가 명세 기반으로 직접 생성** (사용자가 파일을 만들어 넣는 방식은 철회) | 사용자 정정 지시 (2026-09-13) |
| 추가 | 확대 물량 생산 | **Haiku 서브에이전트 병렬 생산 + 이 세션에서 검수** | 사용자 지시 (2026-09-13) |
| 추가 | 애드센스 | **오픈 초반에는 광고 없음.** 설정만 미리 넣어두고 `enabled: false`로 대기 | 사용자 지시 (2026-09-13) |
| 추가 | 서식 디자인 | **관공서풍 격자 대신 현대적 스타일** (딥 슬레이트 네이비 강조, 얇은 테두리, 악센트 바) | 사용자 지시 "가장 최근 세련된 서식" |

---

## 3. 완료된 산출물

### 3.1 서식 46종 (모두 3형식 + 미리보기 + 카탈로그 등록 완료)

경고 0건, 1페이지 34종 / 2페이지 12종.

| 분류 | 종수 | 서식 |
|---|---|---|
| 기업 · 인사·노무 | 10 | 표준근로계약서, 사직서, 재직증명서, 경력증명서, 휴가신청서, 출장신청서, 연차사용계획서, 인사발령통지서, 근로자명부, 경위서 |
| 기업 · 총무·행정 | 6 | 회의록, 업무일지, 기안서, 공문(대외발송), 비품구매요청서, 차량운행일지 |
| 기업 · 회계·세무 | 7 | 견적서, 거래명세서, 지출결의서, 경비정산서, 급여명세서, 영수증, 발주서 |
| 법률 · 계약 | 3 | 차용증(금전소비대차계약서), 용역계약서, 비밀유지계약서(NDA) |
| 법률 · 통지·증명 | 5 | 위임장, 내용증명, 합의서, 각서, 사실확인서 |
| 공공 · 민원·신고 | 2 | 정보공개청구서, 민원신청서 |
| 공공 · 신청·동의 | 4 | 개인정보 수집·이용 동의서, 초상권 이용 동의서, 보호자 동의서, 참가신청서 |
| 부동산 · 임대·매매 | 1 | 주택임대차계약서 |
| 개인 · 취업·경력 | 2 | 이력서(기본형), 자기소개서(기본형) |
| 개인 · 생활·가정 | 3 | 가계부(월간), 여행계획표, 체크리스트(범용) |
| 교육 · 학교·학원 | 3 | 학습계획표(주간), 출석부, 상담일지 |
| **미등록(0종)** | — | 기업·영업·마케팅, 기업·창업·법인, 법률·소송·민원, 의료·복지·동의·신청 |

메인 노출 대표 10종(순서 고정): 이력서 → 자기소개서 → 사직서 → 재직증명서 → 경력증명서
→ 위임장 → 표준근로계약서 → 주택임대차계약서 → 견적서 → 차용증

### 3.2 제작 파이프라인

명세(YAML) 1개 → DOCX·PDF·HWPX·미리보기(WebP)·카탈로그가 전부 자동 생성된다.

- `scripts/form_spec.py` — 명세 로더. YAML 불리언 함정(`No`→False) 차단 포함
- `scripts/render_docx.py` — 명세 → DOCX (python-docx)
- `scripts/render_hwpx.py` — 명세 → HWPX (python-hwpx, 한컴오피스 불필요)
- `scripts/build_form.py` — 전체 파이프라인 + **양방향 자동 맞춤** + 자동 검수
- `scripts/check_spec.py` — 명세 문법·분류·병합범위 검사 (렌더링 없이 빠름)
- `scripts/fit_one_page.py` — 페이지 초과 서식의 입력행·자유기술란을 줄여 자동 보정
- `scripts/validate_catalog.py` — 배포 전 최종 검증 (7개 항목)
- `scripts/export_to_folders.py` — 한글 카테고리 폴더로 내보내기
- `scripts/SPEC_GUIDE.md` — **서식 추가 시 필독**. 블록 타입 레퍼런스와 설계 원칙

지원 블록 타입 12종: `doc_title` `subtitle` `para` `grid` `table` `textbox` `kv_list`
`article` `date_line` `sign_line` `notice` `spacer` `page_break`

### 3.3 사이트

- `scripts/build_site.py` + `templates/` (base·index·category·form·download·_card·_ad)
- 생성 결과: 63페이지 (메인 1 + 카테고리 15 + 상세 46 + 404) + 검색인덱스 + 사이트맵 + robots + favicon
  (광고와 다운로드 중간 페이지를 켜면 138페이지가 추가되어 201페이지)
- 도메인: `https://freeforms.kr` — canonical·sitemap·JSON-LD에 반영
- 메인: 히어로 검색 + 대표 10종 카드 + 최근 추가 10종 + 카테고리 바로가기
- 카테고리: 좌측 중분류 목록(종수 표시) + 4열 카드 그리드
- 상세: 미리보기 확대 + 3형식 다운로드 버튼(파일 크기 표기) + 작성 요령 + 관련 서식 + JSON-LD
- 검색: 빌드 시 만든 `search-index.json`을 브라우저에서 받아 클라이언트 검색 (서버 불필요)
- **저장 파일명은 한글**: URL은 영문 ID를 유지하고 링크의 `download` 속성에 서식 제목을
  넣어, 사용자는 `이력서 (기본형).pdf`로 저장받는다(브라우저 실측 확인 완료).
  URL에 한글을 넣지 않는 이유는 공유·검색에 불리하기 때문이다
- 반응형 확인 완료 (1440px / 400px 스크린샷 검수)

### 3.4 애드센스 대비 (코드는 심어두고 꺼둔 상태)

`catalog/ads.json` 하나로 제어하며 기본값은 **완전 OFF**다. 이 상태에서는 애드센스
스크립트도 광고 자리도 페이지에 나가지 않는다. 슬롯 7곳을 미리 심어 두었고 각 슬롯
컨테이너에 최소 높이를 예약해 두어, 나중에 켜도 레이아웃이 밀리지 않는다(CLS 방지).

| 슬롯 | 위치 | PC | 모바일 |
|---|---|---|---|
| `main_top` | 메인 히어로 아래 | O | O |
| `main_mid` | 대표 10종 ↔ 최근 추가 사이 | O | O |
| `category_side` | 카테고리 사이드바 하단 (세로형) | O | 자동 숨김 |
| `category_bottom` | 카테고리 그리드 아래 | O | O |
| `detail_body` | 상세 화면 태그 아래 | O | O |
| `download_page` | 다운로드 중간 페이지 | O | O |
| `footer` | 푸터 위 | O | O |

페이지당 실제 노출은 2~3개다. `download_interstitial: true`로 바꾸면 다운로드가
`/download/{id}/{형식}/` 중간 페이지를 경유하고 **파일은 2초 후 자동으로 내려간다.**
광고를 보거나 닫을 필요가 없어 정책상 안전하며, 이 페이지는 설정을 켤 때만 생성된다.
`client`를 입력하면 `ads.txt`도 자동 생성된다.

광고 ON 상태로 빌드·브라우저 검수를 마쳤다(201페이지, 메인·상세·중간페이지·모바일 확인).

### 3.5 운영·배포

- `deploy_forms.ps1` — **[로컬]** 빌드 → 사이트 생성 → 검증 → 커밋 → 푸시 → 배포 확인.
  마지막 줄에 주황색 실행일시·구분자 출력. `-Pull`로 에이전트 커밋을 로컬 정본에 반영
- `setup_forms.ps1` — **[로컬]** 최초 설치 (압축 해제 + 패키지 설치 + 외부 도구 점검 + 동작 확인)
- `scripts/agent/daily_forms_agent.md` — 일일 에이전트 지침 정본 (10개 절)
- `_cloudflare_build.md` — Cloudflare Pages 설정값
- `requirements.txt` — Cloudflare 빌드는 jinja2만 필요 (서식 파일은 저장소에 커밋됨)

### 3.6 검증 결과

```
명세 검사        46건 / 문제 0건
서식 빌드        46종 / 실패 0 / 경고 0건
사이트 생성      63페이지
최종 검증        서식 46종 · 배포 파일 272개 · 경고 0건 · 오류 0건 → 배포 가능
브라우저 검수    메인·카테고리·상세·모바일 스크린샷 확인 완료
HWPX 무결성      validate_package 통과 · 46개 전수 점검(세로 A4·맑은 고딕 등록) 문제 0개
HWPX 육안 확인   한글 프로그램으로 확인 완료 (1/1쪽, 세로 A4, 표 폭·남색 머리글 정상)
광고 ON 시험     201페이지 생성 · 레이아웃 정상 · ads.txt 생성 확인 후 OFF로 복원
```

---

## 4. 지금 바로 해야 할 일 (한 단계씩)

### 1단계 — 설치 [로컬]

`C:\Project_Forms`에 `Project_Forms_bundle.zip`과 `setup_forms.ps1`을 이미 넣어두었습니다.

```powershell
cd C:\Project_Forms
.\setup_forms.ps1
```

결과에서 **[필요]** 표시가 나오면 해당 도구(LibreOffice / Poppler / Git)를 설치하고
다시 실행하십시오. 설치가 끝나면 결과를 알려주십시오.

> 확인 포인트: 마지막에 "서식 46종"과 "검증 통과"가 나와야 합니다.

### 2단계 — 로컬에서 사이트 눈으로 확인 [로컬]

```powershell
cd C:\Project_Forms\public
python -m http.server 8000
```

브라우저에서 `http://localhost:8000` 을 열어 메인·카테고리·상세 화면과 다운로드 3형식을
확인하십시오. 한글(HWPX) 육안 확인은 2026-09-13에 완료되었습니다(6.1-1 참조).

### 3단계 — GitHub 저장소 연결 [로컬]

GitHub에서 빈 저장소(프라이빗 가능)를 하나 만드신 뒤 주소를 알려주시면, 제가 명령을
한 줄로 정리해 드리겠습니다. 직접 하시려면:

```powershell
cd C:\Project_Forms
git init
git add -A
git commit -m "init: 무료서식 다운로드 — 서식 46종, 사이트 63페이지"
git branch -M main
git remote add origin <저장소 URL>
git push -u origin main
```

### 4단계 — Cloudflare Pages 연결 [웹 콘솔]

Cloudflare 대시보드 → Workers & Pages → Create → Pages → Connect to Git →
해당 저장소 선택 후 `_cloudflare_build.md`의 설정값을 입력합니다.

| 항목 | 값 |
|---|---|
| Framework preset | None |
| Build command | `pip install -r requirements.txt && python scripts/build_site.py` |
| Build output directory | `public` |
| Environment variable | `PYTHON_VERSION` = `3.11` |

### 5단계 — 도메인 연결 [웹 콘솔 + 로컬]

`freeforms.kr`을 Cloudflare Pages의 Custom domains에 추가합니다. `SITE_URL`은 이미
`https://freeforms.kr`로 반영되어 있습니다. 로컬에서 `site.url` 파일에 다음 한 줄을
적어두면 배포 후 자동으로 200 응답을 확인합니다.

```
https://freeforms.kr
```

### 6단계 — 에이전트 스케줄 작업 등록

텔레그램 신규 봇을 만들어 토큰과 chat_id를 준비해 주십시오(**대화에 붙여넣지 마시고**
스케줄 작업의 비밀변수로 등록합니다). 준비되면 제가 매일 08:00 KST 스케줄 작업을
등록하겠습니다. 지침 정본은 `scripts/agent/daily_forms_agent.md`입니다.

---

## 5. 잔여업무 전체 목록

### 5.1 즉시 (1주 내)

| No | 업무 | 담당 | 비고 |
|---|---|---|---|
| A1 | `setup_forms.ps1` 실행 및 결과 확인 | jimmy | 4장 1단계 |
| A2 | ~~HWPX 한글 육안 확인~~ | jimmy | **완료 (2026-09-13).** 가로 용지·글꼴·행 높이 결함 3건을 찾아 수정 후 재확인 정상 |
| A3 | GitHub 저장소 생성·연결 | jimmy + Claude | 4장 3단계 |
| A4 | Cloudflare Pages 연결 | jimmy | 4장 4단계 |
| A5 | Cloudflare에 도메인 freeforms.kr 연결 | jimmy | `SITE_URL`은 이미 반영 완료 |
| A6 | 텔레그램 신규 봇 생성 (토큰은 비밀변수로) | jimmy | 매매봇 봇과 분리 |
| A7 | 스케줄 작업(매일 08:00 KST) 등록 | Claude | A6 완료 후 |

### 5.2 서식 확충 (2~3주)

| No | 업무 | 현황 |
|---|---|---|
| B1 | **비어 있는 중분류 4개 채우기 (최우선)** | 기업·영업·마케팅 0종, 기업·창업·법인 0종, 법률·소송·민원 0종, 의료·복지 0종 |
| B2 | 기획서 초기 목표 80~100종까지 확충 | 현재 46종 → 약 40종 추가 필요 |
| B3 | 기간제·단시간 근로계약서 추가 | 현재 정규직용만 있음. 표준근로계약서 notice에 안내만 되어 있는 상태 |
| B4 | 상가임대차계약서, 부동산매매계약서 추가 | 부동산 분류가 1종뿐 |
| B5 | 영문 서식(Cover Letter, Invoice 등) 검토 | 기획서 2.1에 후보로 있음. 우선순위 낮음 |

B1·B2는 Haiku 서브에이전트 병렬 생산 + 이 세션 검수 방식이 검증되었으므로 동일하게
진행하면 됩니다. 1회 배치 = 에이전트 6개 × 6종 = 36종, 소요 약 2분(명세) + 약 15분(빌드).

### 5.3 품질 보완

| No | 업무 | 상세 |
|---|---|---|
| C1 | 2페이지 서식 12종 재검토 | 자기소개서·계약서류는 정상. **개인정보 동의서, 정보공개청구서, 민원신청서, 보호자 동의서, 초상권 동의서, 가계부**는 구성을 손보면 1페이지로 줄일 수 있음 |
| C2 | 체크리스트 명세의 제목칸 개선 | `grid` 단일 행이 전체 폭 라벨로 렌더링되어 어색함 |
| C3 | 내용증명 요구사항 칸 확대 | `fit_one_page.py`가 자유기술란을 14mm까지 줄인 상태 |
| C4 | 법정서식 근거 법령 재확인 | `source: law-standard` 2종(표준근로계약서, 주택임대차계약서). 분기 점검 대상 |
| C5 | 서식 하단 출처 표기 검토 | 현재 모든 서식 하단에 "무료서식 다운로드" 7.5pt 회색. 제거하려면 `render_docx.py`의 `SITE_NAME = ""` |

### 5.4 애드센스 (초반 미적용 → 승인 후)

광고 코드와 슬롯은 모두 심어두었고 `catalog/ads.json`의 `enabled: false`로 꺼져 있다.
이 상태에서는 애드센스 스크립트도 광고 자리도 전혀 나가지 않는다.

| No | 업무 | 상세 |
|---|---|---|
| E1 | 콘텐츠 확충 후 애드센스 신청 | 서식 수가 많고 각 상세 화면에 '작성 요령' 본문이 있어야 승인에 유리하다. 5.2의 B1·B2를 먼저 끝내는 것을 권한다 |
| E2 | 승인 후 `client`와 슬롯 ID 입력 → `enabled: true` | `client` 입력 시 `ads.txt`가 자동 생성된다 |
| E3 | `download_interstitial` 켜기 여부 결정 | 켜면 다운로드가 중간 페이지를 경유하고 파일은 2초 후 자동 다운로드된다. 수익은 오르지만 클릭이 한 단계 늘어난다. 2주 시범 후 이탈률을 보고 결정 |
| E4 | 개인정보처리방침·광고 고지 페이지 추가 | 애드센스는 쿠키 사용 고지를 요구한다. 현재 푸터에 면책 문구만 있다 |
| E5 | 슬롯별 성과 확인 후 정리 | 성과가 낮은 슬롯은 ID를 비워 끄면 된다(코드 수정 불필요) |

**정책 리스크 (반드시 지킬 것).** 광고를 닫아야 다운로드가 되는 구조, 다운로드 버튼
바로 옆·아래 광고 배치는 계정 정지 사유다. 현재 구현은 상세 화면 광고를 다운로드
박스에서 최소 38px 떨어뜨리고, 중간 페이지에서는 파일이 자동으로 내려가도록 했다.
이 두 가지를 바꾸지 말 것.

### 5.5 사이트 기능 (Phase 5)

| No | 업무 | 상세 |
|---|---|---|
| D1 | 네이버 서치어드바이저·구글 서치콘솔 등록 | `base.html`에 `naver-site-verification` 메타 태그 자리를 비워 두었음 |
| D2 | Cloudflare Web Analytics 연결 | 무료·쿠키 미사용 |
| D3 | 다운로드 카운트 집계 (Worker + KV) | 카탈로그의 `downloads` 필드가 이미 준비되어 있고 정렬에 반영됨. 값만 채우면 인기순 정렬이 동작함 |
| D4 | 인기순 기반 대표 10종 자동 교체 | D3 완료 후. `featured_rank`를 통계로 갱신 |
| D5 | 사용자 요청 서식 접수 폼 | 정적 사이트이므로 외부 폼 서비스 연동 필요 |
| D6 | 파일 수 20,000개 도달 시 R2 이관 | 현재 272개. 서식 1종당 5~6파일 기준 약 3,500종까지 여유 |

---

## 6. 식별된 이슈와 제약 (중요)

### 6.1 형식 간 자동 변환은 불가능하다 — 확인 완료

| 입력 | PDF 생성 | DOCX 생성 | HWPX 생성 |
|---|---|---|---|
| DOCX | 가능 (LibreOffice) | — | **불가** |
| HWPX / HWP | **불가** | **불가** | — |
| PDF | **불가** | **불가** | **불가** |

LibreOffice로 HWPX를 열게 시도했으나 `source file could not be loaded`로 즉시 실패했고,
한글 포맷으로 내보내는 변환기는 존재하지 않습니다(`python-hwpx`의 ingest는 HWPX→Markdown
단방향만 지원). 따라서 **완성 파일 1개를 넣어 3형식을 채우는 운영 방식은 불가능**하며,
현재의 "명세 1개에서 DOCX·HWPX를 각각 처음부터 생성" 방식이 3형식 제공과 일일 자동 추가를
동시에 만족시키는 유일한 경로입니다. 이 제약은 향후에도 방식 변경 요구가 있을 때마다
재확인 없이 이 문서를 근거로 삼으면 됩니다.

### 6.1-1 HWPX 결함 3건 (2026-09-13 발견·수정, 한글에서 재확인 정상)

사용자가 한글 프로그램으로 열어 확인한 결과 다음 결함이 드러났고 모두 고쳤다.
PDF·DOCX는 처음부터 정상이었으며 HWPX 생성기만의 문제였다.

1. **가로 용지로 열림 (가장 치명적).** 한글의 기본 세로 A4 문서는 `pagePr` 속성이
   `landscape="WIDELY"`다. 이름만 보고 "세로=NARROWLY"로 넣었더니 한글이 가로 용지로
   열었고, 그 결과 표가 좁아 보였고(실제 폭은 정확했다) 페이지 높이가 297mm→210mm로
   줄어 1페이지 서식이 2페이지로 밀렸다. → `set_page_size`에 `orientation`을 **넘기지
   않는다.** 기본 템플릿이 이미 세로 A4다.
2. **글꼴 지정이 무시됨.** 문서의 글꼴 목록(`fontfaces`)에 등록되지 않은 글꼴명을
   `add_run(font=...)`에 넘기면 **조용히 무시**되고 기본 글꼴(함초롬바탕)로 표시된다.
   → 문서 생성 직후 `header.ensure_font("맑은 고딕", lang=...)`으로 7개 언어 슬롯에
   등록한다. HWPX에서는 영문명 "Malgun Gothic"이 아니라 **"맑은 고딕"**을 쓴다.
3. **행 높이 미지정.** HWPX는 행이 아니라 셀이 높이를 가지며, 지정하지 않으면 기본
   3600 HWPUNIT(12.7mm)이 적용되어 표가 과도하게 커진다. → 행마다 `cell.set_size(height=)`
   를 적용하고, 표의 `hp:sz height`도 셀 합계로 다시 맞춘다(어긋나면 레이아웃이 틀어진다).

### 6.2 이 세션에서 겪은 함정 (다음 세션이 반복하지 말 것)

1. **HWPX의 용지 설정(`secPr`)은 첫 문단의 run 안에 들어 있다.**
   새 문서의 기본 빈 문단을 삭제하면 A4·여백 설정이 함께 사라진다.
   → `render_hwpx.py`는 첫 문단을 삭제하지 않고 첫 블록이 재사용하도록 구현했다.
2. **python-docx의 `trHeight`를 셀마다 설정하면 행 높이가 틀어진다.**
   `trPr`에 `trHeight`가 중복 추가되기 때문. → 행 단위로 1회만 설정(`_row_height`).
3. **`tcPr` 자식 요소의 순서는 스키마로 강제된다.** `tcBorders` → `shd` → `vAlign` 순.
4. **python-docx의 셀 병합은 각 셀의 빈 문단을 누적시킨다.** 병합 후 첫 문단만 남기고
   지워야 행 높이가 비정상적으로 커지지 않는다.
5. **YAML은 따옴표 없는 `No`/`Yes`/`On`/`Off`를 불리언으로 읽는다.**
   표 머리글 `No`가 `False`로 출력되는 사고가 실제로 발생했다.
   → `form_spec.py`에서 명세 단계에 차단하고 `SPEC_GUIDE.md`에 명시했다.
6. **`pdftotext -layout`은 검수에 쓸 수 없다.** 셀 안에서 줄바꿈된 글자 사이에 옆 칸
   글자를 끼워 넣어 항목 존재 확인이 실패한다. → 읽기 순서를 보존하는 `-raw`를 사용한다.
7. **자동 맞춤의 페이지 채움 비율 계산 시 하단 푸터를 제외해야 한다.**
   푸터가 아래 여백 안에 있어서 채움 비율이 항상 100%로 나왔다.
8. **카탈로그는 서식마다 저장해야 한다.** 마지막에 한 번만 저장하면 중간에 중단될 때
   진행분이 전부 사라진다(실제로 46종 빌드 중 발생).
9. **LibreOffice 병렬 실행은 프로세스별 사용자 프로필이 필요하다.**
   `-env:UserInstallation=file:///tmp/lo_profile_<pid>` 를 붙이면 4개 동시 실행이 가능하고
   전체 빌드 시간이 약 1/4로 줄어든다.
10. **로컬 Filesystem MCP 도구가 이 세션에서 스키마 오류로 동작하지 않았다.**
    (`JSON Schema draft-07 dialect` 미지원) → `device_request_folder_access` +
    `device_commit_files`(stagedPath/fileUuid) 경로로 우회했다. 다음 세션에서도 같은
    오류가 나면 같은 방법을 쓰면 된다.

### 6.3 위험 항목

| 항목 | 영향 | 대응 |
|---|---|---|
| `SITE_URL` 미수정 상태로 배포 | canonical·sitemap이 `example.com`을 가리켜 SEO가 무력화됨 | 도메인 연결과 동시에 수정 (A5) |
| HWPX 육안 미검증 | 한글에서 표가 깨질 가능성 (구조·검증은 통과) | A2에서 반드시 확인 |
| 에이전트 생성 서식 품질 편차 | 신뢰도 저하 | 승인 단계 + 자동 검수 + 미리보기 육안 확인 3중 장치. 법률성 강한 서식은 사람이 검수 |
| 법정서식 개정 미반영 | 잘못된 서식 배포 | 분기 1회 점검을 에이전트 지침 8절에 명시 |
| 토큰 노출 | 보안 | 스케줄 작업 비밀변수로만 보관. 저장소·문서·리포트에 기재 금지 |

---

## 7. 개발가이드 반영 필요 항목

본 프로젝트는 자동매매봇 개발가이드(GUIDEBOOK)와 별개 라인이지만, 아래 항목은 가이드에
반영하면 다른 프로젝트에서도 재사용할 수 있습니다.

1. **Haiku 서브에이전트 병렬 생산 + 상위 모델 검수 패턴** — 반복적 명세 작성 작업에 적용.
   핵심은 (a) 규칙서를 먼저 문서로 고정, (b) 검증 스크립트를 먼저 만들어 에이전트가
   스스로 통과 여부를 확인하게 함, (c) 빌드·최종 검수는 상위 모델이 중앙에서 수행.
   이번에 36종을 약 2분에 생산했고 문법 오류는 검증 스크립트가 전부 잡았다.
2. **"검증 스크립트를 먼저 만든다" 원칙** — `check_spec.py`가 병합 범위 검사를 빠뜨려
   빌드가 중단되는 사고가 있었다. 검증기의 빈틈이 곧 생산 중단으로 이어진다.
3. **증분 저장 원칙** — 배치 작업의 결과물은 항목마다 저장한다.
4. **외부 도구 병렬 실행 시 프로필 분리** — LibreOffice 사례.

---

## 8. 참고 — 자주 쓰는 명령 요약

```powershell
.\deploy_forms.ps1                      # 배포 (빌드→사이트→검증→커밋→푸시→확인)
.\deploy_forms.ps1 -Pull                # 에이전트 커밋을 로컬 정본에 반영 후 배포
.\deploy_forms.ps1 -SkipBuild           # 서식 재생성 없이 사이트만 갱신·배포

python scripts\check_spec.py            # 명세 문법 검사 (빠름)
python scripts\build_form.py {id}       # 서식 1종 재생성
python scripts\fit_one_page.py --all    # 페이지 초과 서식 일괄 보정
python scripts\build_site.py            # 사이트만 재생성
python scripts\validate_catalog.py      # 배포 전 최종 검증
python scripts\export_to_folders.py     # 한글 카테고리 폴더로 내보내기
```
