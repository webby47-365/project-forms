# 인수인계서 — 직장인 도구 2호 「퇴직금·연차·실수령액 계산기」 (2026-09-14)

작성: Claude (Cowork, 클라우드 세션) · 정본 폴더: `C:\Project_Forms` · 대상 사이트: https://freeforms.kr

## 1. 이번 작업 요약

| 구분 | 내용 | 상태 |
|---|---|---|
| 신규 | `/tools/severance/` 퇴직금 계산기 | 로컬 반영 완료 · **배포 대기** |
| 신규 | `/tools/annual-leave/` 연차 계산기 | 로컬 반영 완료 · **배포 대기** |
| 신규 | `/tools/salary/` 연봉 실수령액 계산기 | 코드 완료 · **간이세액표 파일 대기** |
| 신규 | `catalog/rates_2026.json` — 4대보험·소득세 요율 정본 (연 1회 갱신) | 완료 |
| 신규 | `scripts/build_tax_table.py` — 홈택스 간이세액표 엑셀 → JSON 변환기 | 완료 |
| 변경 | `templates/base.html` 푸터가 도구 4종을 자동으로 나열 | 완료 |
| 변경 | `/tools/` 허브 '준비 중'에서 계산기 제거(이제 실제 페이지) | 완료 |

화면 구성은 **A안(2단 — 왼쪽 입력 / 오른쪽 고정 결과)**으로 승인받아 3종 모두 같은 골격을 쓴다.

## 2. 배포 방법 [로컬 작업]

### 2-1. 지금 바로 (퇴직금·연차 2종)

```powershell
cd C:\Project_Forms
.\deploy_forms.ps1 -Message "feat(tools): 퇴직금·연차 계산기 추가"
```

`-SkipBuild`를 **넣지 않는다.** 이번에는 사이트 HTML을 새로 만들어야 한다(새 페이지 2개).
간이세액표가 없으면 빌드가 다음 줄을 찍고 실수령액 페이지만 건너뛴다 — 오류가 아니다.

```
[사이트] 알림: catalog/tax_table_2026.json 이 없어 /tools/salary/ 는 만들지 않았습니다 (간이세액표 필요).
[사이트] 페이지 291개 · 서식 239종 ...
```

### 2-2. 간이세액표를 받은 뒤 (실수령액 1종)

```powershell
# 1) 홈택스 → 세금신고 → 원천세 신고 → 근로소득 간이세액표 → 전체 파일(엑셀) 내려받아
#    C:\Project_Forms\catalog\ 에 넣는다
cd C:\Project_Forms
python scripts\build_tax_table.py
.\deploy_forms.ps1 -Message "feat(tools): 연봉 실수령액 계산기 추가"
```

`build_tax_table.py` 는 catalog 폴더에서 파일을 알아서 찾고, 읽은 구간 수·표본값을 찍어 준다.
읽기에 실패하면 이유를 한 줄로 알려주니 그 메시지를 그대로 전달하면 된다(엑셀 형식이 예상과 다를 수 있다).

### 배포 후 확인 (라이브)

1. https://freeforms.kr/tools/ — 카드가 4장, '준비 중'은 「금액 한글 표기·글자수 세기」만
2. https://freeforms.kr/tools/severance/ — 입사일·퇴직일을 바꾸면 **임금 입력 칸이 3~4개로 다시 그려지는지**
   (달 경계로 쪼개진다). 금액을 넣으면 오른쪽 결과가 즉시 바뀌는지
3. https://freeforms.kr/tools/annual-leave/ — 입사일 2023-09-15 · 기준일 오늘 → 총 41일이 나오는지.
   '회계연도(1/1) 기준'으로 바꾸면 표가 달라지는지
4. 두 페이지에서 **결과 이미지 저장** → `퇴직금 계산기_결과.png` 가 받아지는지, **계산 결과 복사** → 메모장에 붙여지는지
5. 모바일(폰)에서 2단이 1단으로 접히고 결과가 입력 아래로 내려오는지
6. 상단 메뉴 '직장인 도구' 하위에 계산기 3종이 나오는지, 푸터에도 나오는지
7. 구글 서치콘솔 → 사이트맵 재제출 (URL 287 → 289, 표 탑재 후 290)
8. `python scripts\ping_indexnow.py --all` 한 번 (배포 스크립트의 `--recent 5` 는 도구 URL을 안 집는다)

## 3. 바뀐 파일

| 파일 | 변경 |
|---|---|
| `assets/calc.js` | **신규.** 계산 엔진 전부. 4대보험·간이세액표 조회·퇴직금·연차·결과 이미지·클립보드 |
| `templates/tool_calc.html` | **신규.** 계산기 3종 공통 템플릿. 입력 칸과 결과 표만 `calc.key` 로 갈라진다 |
| `catalog/rates_2026.json` | **신규.** 요율 정본 + 출처 목록. 화면의 퍼센트 표시도 이 값을 읽는다 |
| `scripts/build_tax_table.py` | **신규.** 간이세액표 엑셀/CSV → `catalog/tax_table_2026.json` |
| `scripts/build_site.py` | `TOOLS` 에 3종 + `short` 필드, `CALCULATORS`(화면 글·FAQ·관련 서식), 렌더링 블록, `load_rates()`·`file_version()`, `tools_active`(표 없으면 실수령액을 메뉴·사이트맵에서도 제외), assets 복사에 `calc.js`, `public/assets/tax_table.json` 배포 |
| `templates/base.html` | 푸터가 `tools` 목록을 돌며 도구를 나열(도장만 하드코딩돼 있던 것) |
| `README.md` | 「직장인 도구」 절에 계산기 3종 + '계산기 3종의 데이터(연 1회 갱신)' 소절 |

## 4. 적용한 2026년 요율과 근거

| 항목 | 값(근로자 부담) | 근거 |
|---|---|---|
| 국민연금 | 4.75% (총 9.5%) | 연금개혁으로 2026년부터 인상. 기준소득월액 상한 659만원·하한 41만원(2026.7~2027.6, 국민연금공단 고시) |
| 건강보험 | 3.595% (총 7.19%) | 보건복지부 2025-08-28 건강보험정책심의위원회 |
| 장기요양보험 | 건강보험료 × 13.1405% | 복지부 2025-11-04, 소득 대비 0.9448% ÷ 7.19% |
| 고용보험 | 0.9% | 실업급여 계정 1.8% 노사 절반. **2027년 2.0%로 인상 심의 중**(2026-09-01 고용보험위원회) — 시행되면 `rates_2026.json` 만 고치면 된다 |
| 소득세 | 근로소득 간이세액표 | 소득세법 시행령 별표2 (2026-02-27 개정) |
| 지방소득세 | 소득세 × 10% | |

보험료·세액은 모두 **10원 미만 절사**한다. 산재보험은 전액 사업주 부담이라 근로자 공제에 넣지 않았다.

## 5. 설계 결정 (왜 이렇게 했나)

- **3개 별도 페이지.** 검색어가 "퇴직금 계산기" / "연차 계산기" / "연봉 실수령액"으로 완전히 갈린다.
  한 페이지 탭으로 묶으면 유입도 광고 지면도 1/3이 된다. 대신 상단에 서로를 오가는 칩을 두었다.
- **소득세는 간이세액표 원문을 쓴다.** 처음에는 법정 산식(근로소득공제→인적공제→기본세율→근로소득세액공제)으로
  근사해 봤는데 실제 표보다 **월 5만원가량 높게** 나왔다. 간이세액표에는 공개되지 않은 '특별소득공제 등
  대용 금액'이 들어 있어 산식으로는 재현되지 않는다. 사람인·잡코리아가 표 원문을 넣어 쓰는 이유다.
  숫자가 다르면 방문자가 바로 알아보므로 표를 넣는 쪽을 택했다.
- **표가 없으면 그 페이지만 빠진다.** 빌드를 실패시키면 일일 에이전트의 자동 배포가 통째로 멈춘다.
  메뉴·푸터·사이트맵에서도 함께 빠지므로 링크만 남아 404가 나는 일이 없다.
- **요율을 코드에서 분리.** `rates_2026.json` 하나만 고치면 계산도 화면의 퍼센트 표시도 같이 바뀐다.
  연 1회 갱신을 일일 에이전트에게 맡기지 않았다 — 틀리면 바로 티가 나는 숫자다.
- **개인정보 0.** 급여·입사일 모두 캔버스·자바스크립트 안에서만 처리하고 전송 코드가 없다.
  처리방침 수정 불필요(도장 도구와 같다).
- **애드센스 '얇은 페이지' 회피.** 3종 모두 사용법 5단계 + FAQ 4문항 + 관련 서식 10종을 본문으로 두었고,
  FAQ는 FAQPage 구조화 데이터로도 나간다. WebApplication JSON-LD(FinanceApplication)도 붙는다.
- **퇴직금은 고용노동부와 같은 방식.** 평균임금 산정기간을 달 경계로 쪼개 3~4칸으로 보여 준다.
  "왜 칸이 4개냐"는 질문이 나올 수 있지만, 일수로 나누는 계산이라 이 방식이라야 정확하다.

## 6. 잔여 업무

1. **[로컬] 배포** — 2-1 실행 → 확인 항목 8개.
2. **[로컬] 간이세액표 탑재** — 2-2. 이게 끝나야 실수령액 계산기가 공개된다.
3. **검산** — 표 탑재 후 사람인 연봉계산기(부양가족 1명·비과세 20만원)와 몇 개 연봉으로 대조.
   차이가 100원 이상이면 알려 주기 바란다(비과세 가정이 다를 가능성이 높다).
4. **서치콘솔 사이트맵 재제출** + `ping_indexnow.py --all`.
5. **연 1회 갱신 알림** — 매년 1월(요율)·2월(간이세액표)·7월(연금 상하한). 예약 작업으로 걸어 둘지 결정 필요.
6. 이전부터 남아 있는 것: 애드센스 신청(색인 50페이지 이상 후, 9/17~19 예정) · 검색 오타 허용 ·
   서식 요청 폼 켜기 · 1차 배포 때 저장소에 딸려 올라간 `step3_*`·`fix_*` 파일 정리.

## 7. 식별 이슈

- **고용보험료율이 2027년에 1.0%로 오를 예정**이다(2026-09-01 심의, 법령 개정 진행 중).
  확정되면 `rates_2026.json` 의 `employment.rate_employee` 만 고치면 된다.
- **국민연금 보험료율은 매년 0.5%p씩 오른다**(2026년 9.5% → 2033년 13%). 매년 1월 갱신 대상.
- **간이세액표는 매년 2월경 개정**된다. 개정본을 같은 자리에 넣고 `build_tax_table.py` 를 다시 돌리면 된다.
  파일이 낡아도 빌드는 통과하므로, 연 1회 갱신을 잊지 않도록 예약 작업을 권한다.
- 연차 계산기는 **육아휴직·병가 등 장기 휴직을 반영하지 못한다**(출근율 80% 판정이 필요). 면책 문구에 적었다.
- 퇴직금은 **세전 금액**이다. 퇴직소득세는 근속연수공제·환산급여 구조라 계산기를 따로 만들어야 한다.
  다음 도구 후보로 둘 만하다.

## 8. 이번 세션의 경험치 (가이드 반영)

- **법정 수치는 1차 출처까지 가야 한다.** 블로그·계산기 사이트의 2026년 요율이 서로 달랐다
  (같은 월급에 소득세가 0원 / 9,340원 / 14,890원으로 갈림). 복지부 보도자료·공단 고시·법령 별표까지
  확인한 값만 `rates_2026.json` 에 넣고 출처를 파일 안에 함께 적었다.
- **법령 PDF를 웹 요약으로 읽을 때 표 숫자는 믿지 말 것.** 별표2에서 8개 구간을 읽어 왔는데,
  그 값으로 역산하니 공제액 곡선이 들쭉날쭉해 일부가 잘못 읽힌 것이 드러났다.
  설명 문장(비고·산식)은 정확했으므로, **문장은 받아 쓰고 표는 원본 파일로 받는다**는 기준이 맞다.
- **역산은 검산 도구로 쓸 수 있다.** 공개 계산기의 결과에서 거꾸로 공제액을 풀어 보니 곡선이 매끄럽지 않아
  입력 데이터의 오류를 찾아냈다. 값 하나만 맞춰 보는 것보다 여러 점을 넣고 형태를 보는 편이 낫다.
- **자동 배포 파이프라인이 있는 프로젝트에서는 '없으면 실패' 대신 '없으면 그 부분만 건너뛰기'.**
  데이터 파일 하나가 없다고 빌드를 세우면 일일 에이전트의 무인 배포가 멈춘다. 대신 메뉴·사이트맵에서도
  함께 빼서 깨진 링크가 남지 않게 해야 한다.
- **Jinja 자동 이스케이프.** FAQ 답변에 `<b>` 를 넣었더니 화면에 태그가 글자로 나왔다.
  `| safe` 를 붙인 필드(howto)와 아닌 필드(faq)를 섞어 쓸 때는 **어느 쪽인지 먼저 확인**할 것.
  FAQ는 구조화 데이터로도 나가므로 평문이 맞다.
- **자바스크립트 부동소수점.** `3,300,000 × 0.009 = 29,699.999…` 라서 10원 절사를 하면 29,690원이 됐다.
  금액 절사 함수에는 항상 `+1e-6` 같은 보정을 넣는다.
