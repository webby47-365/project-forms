# 빌드 부산물
**/_tmp_preview/
*.pyc
__pycache__/
.DS_Store
Thumbs.db

# 로컬 전용 설정 (사이트 주소 등)
site.url

# 이관용 임시 압축본 (조각으로 나뉜 것까지 모두 제외)
update_forms*.zip
Project_Forms_bundle.zip

# Claude 데스크톱 앱이 저장한 파일 사본 — 정본이 아니므로 형상관리 대상이 아니다
Claude outputs/

# 같은 이름으로 다시 저장할 때 Windows가 붙이는 사본 (update_forms-1.ps1 등)
update_forms-*.ps1
